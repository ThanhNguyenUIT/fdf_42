<tr>
  <td><%= category.id %></td>
  <td><%= category.name %></td>
  <td>
    <%= link_to t(".edit"), edit_admin_category_path(category), class: "btn btn-primary" %>
    <%= link_to t(".delete"), admin_category_path(category), method: :delete, class: "btn btn-danger" %>
  </td>
</tr>
